// WAP to determine num divisible by 97 or not?
#include <stdio.h>

int main(void) {
    int num;
    printf("Enter the num \n");
    scanf("%d",&num);
    printf("Divisibility test %d \n",num%97);
	return 0;
}

